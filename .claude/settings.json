{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); sid=$(printf '%s' \"$input\" | jq -r '.session_id // empty' | cut -c1-8); msg=$(printf '%s' \"$input\" | jq -r '.prompt // .message // \"\"'); [ -n \"$sid\" ] && printf '%s' \"$msg\" | grep -qE '離開|下班|結束|掰|關掉|關閉|收工|bye|goodbye|要走了|離場|要離開' && touch \"/tmp/km-wrap-ready-$sid\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "KM_VAULT=\"$CLAUDE_PROJECT_DIR\" bash \"$CLAUDE_PROJECT_DIR/tools/hooks/km-auto-promote.sh\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "KM_VAULT=\"$CLAUDE_PROJECT_DIR\" bash \"$CLAUDE_PROJECT_DIR/tools/hooks/km-session-wrap.sh\""
          }
        ]
      }
    ]
  }
}
